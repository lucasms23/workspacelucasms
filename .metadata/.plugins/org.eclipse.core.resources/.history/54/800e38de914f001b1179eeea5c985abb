package decisao;

import javax.swing.JOptionPane;

public class Lista2Exercicio2 {

	public static void main(String[] args) {
/*
		double normal = Double.parseDouble(JOptionPane.showInputDialog("Preço normal de etiqueta: "));
		double opcao =  Double.parseDouble(JOptionPane.showInputDialog("Opcao de pagamento (1 a 4): "));

		if (opcao == 1) {
			total = normal * 0.90;
		}else if (opcao == 2) {
			total = normal * 0.95;
		}else if (opcao == 3) {
			total = normal;
		}else if (opcao == 4) {
			total = normal * 0.95;
		}else {
			System.out.println("Opção Inválida!");
		}
double total = (diaria * qtde) + taxa;

System.out.println("Total a pagar: "+total);
*/

}

}
